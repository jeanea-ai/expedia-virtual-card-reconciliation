{
  "name": "expedia-virtual-card-reconciliation",
  "version": "0.2.3",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "description": "Deterministic extraction and validation engine for the Kolo Expedia Virtual Card Reconciliation skill",
  "type": "commonjs",
  "scripts": {
    "build:schemas": "node scripts/compile_schemas.js",
    "test": "node scripts/compile_schemas.js && node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "linkedom": "^0.18.13"
  }
}
